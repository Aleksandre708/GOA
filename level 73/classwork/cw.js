function addNewElement() {
  const container = document.getElementById('container');
  const buttonText = document.createTextNode("ახალი ღილაკი");
  newButton.appendChild(buttonText);
  container.appendChild(newButton);
}
